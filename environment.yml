name: emit-ghg
channels:
  - conda-forge

dependencies:
  # Core Python
  - python>=3.11

  # Required: GDAL must be installed via conda (not pip)
  - gdal

  # Scientific Computing (all available via conda)
  - numpy>=2.0
  - scipy
  - h5py
  - pytest
  - netcdf4

  # Python packages via pip
  - pip:
      - spectral  # Hyperspectral image processing
      - argparse

